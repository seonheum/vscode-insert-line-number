import * as assert from 'assert';

suite("LineNumberInserter Tests", function () {
    test("Insert line number with default settings", function () {
        // TODO:
        // Implement the integration tests.
        // Don't know how to simulate user select after showing of a QuickPick list.
        assert.ok({}, "Happy pass.");
    });
});